use - docker-compose up (to build and run the image)
use docker ps (list all the running containers)
use docker image ls (list all te docker images created)
use docker kill <container_name> (kill that particular container)